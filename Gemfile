source 'https://rubygems.org'

gem 'roda'
gem 'rake'
gem 'dotenv'
gem 'unicorn'

# Templates
gem 'tilt'

# CSS and JS
gem 'sass'

# MyNeta
gem 'nokogiri'
gem 'sequel'
gem 'pg'

group :test do
  gem 'rack-test'
  gem 'rspec'
  gem 'rspec-given'
end

# Dev only
group :dev do
  gem 'rack-unreloader'
end
